export class Worker {
    constructor(user_id, ZipCode, userName, userEmail, firstName, lastName, phoneNumber, part_or_full_time, skillTags, rating) {
        this.user_id = user_id; 
        this.ZipCode = ZipCode; 
        this.userName = userName; 
        this.userEmail = userEmail; 
        this.firstName = firstName; 
        this.lastName = lastName; 
        this.phoneNumber = phoneNumber; 
        this.part_or_full_time = part_or_full_time; 
        this.skillTags = skillTags; 
        this.rating = rating; 
    }
}
